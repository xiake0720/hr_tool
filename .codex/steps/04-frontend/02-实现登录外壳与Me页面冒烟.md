# Step 20：实现登录外壳（占位）+ Me 页面冒烟

## CODEX 执行指令（必须严格照做）
先实现“登录壳子”，不要求真正发起 OAuth 登录（本地可用手动粘贴 token），目的是前后端链路跑通。UTF-8（无 BOM）。

### 任务清单
1）实现 /login 页面：
- 一个输入框：token
- 一个按钮：保存 token 到 localStorage
  2）apiClient 请求时自动带上 Authorization Bearer <token>
  3）/me 页面展示：
- externalSub、username/email、requestId
  -（后续接入 DB 映射后再展示 tenantId/userId）

### 交付物
- LoginPage.vue
- 路由配置与导航守卫（无 token 则跳转 /login）
- apiClient 自动附加 token

### 验收（你必须给出）
- 启动前端，输入 token 后访问 /me，能成功拿到数据并展示
